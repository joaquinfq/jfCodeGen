{{#each methods}}
    /**
  {{#if desc}}
    {{#each desc}}
     * {{{tr this}}}
    {{/each}}
  {{/if}}
  {{#if override}}
     * {{{tr "Método heredado de `%s`." @root/base}}}
     *
     * @override
  {{/if}}
     *
     * @method {{name}}
  {{#if params}}
     *
    {{#each params}}
     * @param {{doc-type typePad}} {{namePad}} {{{desc}}}
    {{/each}}
  {{/if}}
  {{#if return}}
     *
     * @return {{return}}
  {{/if}}
     *
     {{scope name}}
     */
    {{name}}({{#each params}}{{name}}{{#unless @last}}, {{/unless}}{{/each}})
    {
  {{#if before}}
        super.{{name}}({{#each params}}{{name}}{{#unless @last}}, {{/unless}}{{/each}});
  {{/if}}
        {{{body}}}
  {{#if after}}
    {{#if returns}}
        return super.{{name}}({{#each params}}{{name}}{{#unless @last}}, {{/unless}}{{/each}});
    {{else}}
        super.{{name}}({{#each params}}{{name}}{{#unless @last}}, {{/unless}}{{/each}});
    {{/if}}
  {{/if}}
    }
{{/each}}
