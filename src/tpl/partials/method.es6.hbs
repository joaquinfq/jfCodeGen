    /**
{{#if desc}}
    {{#each desc}}
     * {{{tr this}}}
    {{/each}}
{{/if}}
{{#if getter}}
    {{#if override}}
     *
     * @override
    {{/if}}
     *
     * @property {{name}}
     * @type     {{{return}}}
{{/if}}
{{#unless getter}}
    {{#if override}}
     * {{{tr "Método heredado de `%s`." @root/base}}}
     *
     * @override
    {{/if}}
     *
     * @method {{name}}
    {{#if params}}
     *
        {{#each params}}
     * @param {{doc-type typePad}} {{namePad}} {{{desc}}}
        {{/each}}
    {{/if}}
    {{#if return}}
     *
     * @return {{{return}}}
    {{/if}}
{{/unless}}
     *
{{#if static}}
     * @static
{{/if}}
     {{scope name}}
     */
    {{#if static}}static {{/if}}{{#if getter}}get {{/if}}{{name}}({{#each params}}{{name}}{{#unless @last}}, {{/unless}}{{/each}})
    {
{{#if before}}
        super.{{name}}({{#each params}}{{name}}{{#unless @last}}, {{/unless}}{{/each}});
{{/if}}
        {{{body}}}
{{#if after}}
    {{#if return}}
        return super.{{name}}({{#each params}}{{name}}{{#unless @last}}, {{/unless}}{{/each}});
    {{else}}
        super.{{name}}({{#each params}}{{name}}{{#unless @last}}, {{/unless}}{{/each}});
    {{/if}}
{{/if}}
    }
