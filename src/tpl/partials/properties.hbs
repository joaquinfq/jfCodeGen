{{#each properties}}
        /**
{{#if override}}
         * @inheritDoc
{{else}}
{{#each desc}}
{{#if this}}
         * {{{this}}}
{{else}}
         *
{{/if}}
{{/each}}
         *
         * @property {{name}}
         * @type     {{type}}
{{#if readonly}}
         * @readOnly
{{/if}}
{{/if}}
         */
        '{{name}}' : {{{format this}}},
{{/each}}
{{>computed}}
{{>observers}}
